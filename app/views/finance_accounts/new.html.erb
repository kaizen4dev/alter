<%= turbo_frame_tag "content", data: {turbo_action: :advance} do %>
  <div class="container p-4 my-4 is-max-tablet">
    <%= tag.h1 "New #{params[:group]} account", class: "title" %>
    <%= render "form" %>
  </div>
<% end %>
